<Window x:Class="ShoppingList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShoppingList"
        mc:Ignorable="d"        
        d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel}"
        Title="MainWindow" Height="600" Width="450" FontFamily="Lucida" FontSize="24">
    
    <Grid Background="DimGray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="ShoppingListBox" Grid.Row="0" Grid.Column="0" Background="WhiteSmoke"
                 ItemsSource="{Binding MyShoppingList}" SelectedItem="{Binding SelectedShopItem}">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type local:ShopItem}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBox Text="{Binding NewName}" Grid.Row="1" Background="LightGray" BorderThickness="1" BorderBrush="DimGray"/>
        <Button Content="Add Item" Command="{Binding AddItem}" CommandParameter="{Binding Text, ElementName=NewItem}" Grid.Row="2" Background="LightSlateGray"/>
    </Grid>
</Window>
